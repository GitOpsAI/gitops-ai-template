---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: external-dns
namespace: external-dns
resources:
  - namespace.yaml
  - helm-repository-external-dns.yaml
  - helm-release-external-dns.yaml
configMapGenerator:
  - name: external-dns-values-base
    files:
      - values.yaml=values.yaml
configurations:
  - ../../../kustomizeconfig.yaml
