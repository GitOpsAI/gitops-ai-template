apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: cert-manager
namespace: cert-manager
resources:
  - namespace.yaml
  - helm-repository-jetstack.yaml
  - helm-release-cert-manager.yaml
  - helm-release-additional-manifests.yaml
configurations:
  - ../../../kustomizeconfig.yaml
