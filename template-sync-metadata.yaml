# Machine-readable template identity. Bump `version` on each tagged release (semver).
# Keep in sync with CHANGELOG.md and git tags.
version: "1.1.1"
schema: 1
upstream:
  provider: github
  path: GitOpsAI/gitops-ai-template
  host: github.com
